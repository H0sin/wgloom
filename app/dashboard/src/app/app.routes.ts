import { Routes } from '@angular/router';
import { AddInterfaceComponent } from './components/interface/add-interface/add-interface.component';

export const routes: Routes = [
    {
        path : 'add-interface',
        component:AddInterfaceComponent 
    }
];
